<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Your Password</title>
    <style>
        /* Reset styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background-color: #f8fafc;
            line-height: 1.6;
            color: #334155;
        }

        /* Main container */
        .email-wrapper {
            max-width: 600px;
            margin: 40px auto;
            background: #ffffff;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }

        /* Header section with gradient */
        .email-header {
            background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
            padding: 40px 20px;
            text-align: center;
            position: relative;
        }

        .success-icon {
            width: 120px;
            height: 120px;
            background: #ffffff;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .success-icon img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }

        .header-title {
            color: #ffffff;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .header-subtitle {
            color: rgba(255, 255, 255, 0.9);
            font-size: 16px;
        }

        /* Content section */
        .email-content {
            padding: 40px 30px;
        }

        .info-box {
            background: #f1f5f9;
            border-radius: 12px;
            padding: 20px;
            margin: 20px 0;
        }

        .reset-link {
            word-break: break-all;
            color: #3b82f6;
            text-decoration: none;
            font-family: monospace;
            font-size: 14px;
            background: #f8fafc;
            padding: 12px;
            border-radius: 6px;
            border: 1px solid #e2e8f0;
            display: block;
            margin: 10px 0;
        }

        /* CTA Button */
        .cta-button {
            display: inline-block;
            background: #3b82f6;
            color: #ffffff !important;
            padding: 14px 28px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            margin: 20px 0;
            text-align: center;
            transition: background-color 0.3s ease;
        }

        .cta-button:hover {
            background: #2563eb;
        }

        .button-container {
            text-align: center;
            margin: 30px 0;
        }

        /* Footer section */
        .email-footer {
            background: #f8fafc;
            padding: 20px;
            text-align: center;
            border-top: 1px solid #e2e8f0;
        }

        .footer-links {
            margin-bottom: 15px;
        }

        .footer-link {
            color: #3b82f6;
            text-decoration: none;
            margin: 0 10px;
        }

        .footer-text {
            color: #64748b;
            font-size: 14px;
        }

        .warning-text {
            color: #dc2626;
            font-size: 14px;
            margin-top: 20px;
            padding: 12px;
            background: #fee2e2;
            border-radius: 8px;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            .email-wrapper {
                margin: 20px;
                border-radius: 12px;
            }

            .email-header {
                padding: 30px 15px;
            }

            .email-content {
                padding: 30px 20px;
            }

            .success-icon {
                width: 300px;
                height: 300px;
            }
        }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <!-- Header Section -->
        <div class="email-header">
            <div class="success-icon">
                <img src="https://e7.pngegg.com/pngimages/511/969/png-clipart-red-and-black-rocket-illustration-rocket-spacecraft-rocket-image-file-formats-cartoon.png" alt="Password Reset Icon">
            </div>
            <h1 class="header-title">Reset Your Password</h1>
            <p class="header-subtitle">Follow the instructions below to set a new password</p>
        </div>

        <!-- Content Section -->
        <div class="email-content">
            <p>Hello,</p>
            
            <p>We received a request to reset the password for your account (<strong><%= email %></strong>). To proceed with the password reset, click the button below:</p>

            <div class="button-container">
                <a href="<%= resetLink %>" class="cta-button">Reset My Password</a>
            </div>

            <div class="info-box">
                <p>If the button above doesn't work, copy and paste this link into your browser:</p>
                <a href="<%= resetLink %>" class="reset-link"><%= resetLink %></a>
            </div>

            <p class="warning-text">
                ⚠️ If you didn't request a password reset, please ignore this email or contact our support team immediately.
            </p>

            <p>Best regards,<br>TicketFlow Support Team</p>
        </div>

        <!-- Footer Section -->
        <div class="email-footer">
            <div class="footer-links">
                <a  class="footer-link">Help Center</a>
                <a  class="footer-link">Contact Support</a>
            </div>
            <p class="footer-text">&copy; <%= new Date().getFullYear() %> TicketFlow. All rights reserved.</p>
        </div>
    </div>
</body>
</html>