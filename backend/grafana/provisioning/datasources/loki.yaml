apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    jsonData:
      maxLines: 1000
      derivedFields:
        - name: "service"
          matcherRegex: "service=\"(.*?)\""
          url: "http://localhost:3000/explore?orgId=1&left=[\"now-1h\",\"now\",\"Loki\",{\"expr\":\"{service=\"$1\"}\"}]"